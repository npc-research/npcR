% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latest_acs_year.R
\name{latest_acs_year}
\alias{latest_acs_year}
\title{Find the most recent available ACS year}
\usage{
latest_acs_year(product = "acs5", frequency = "yearly")
}
\arguments{
\item{product}{Character scalar. ACS product to test. Common values include
\code{"acs1"} and \code{"acs5"}.}

\item{frequency}{Character scalar. ACS frequency to test. Supported values
include \code{"yearly"}, \code{"annually"}, \code{"fiveyears"}, \code{"quinquennial"}, and
\code{"decennial"}.}
}
\value{
An integer giving the most recent ACS year available for the
requested \code{product} and \code{frequency}.
}
\description{
Searches backward from the current calendar year to find the latest year for
which ACS variables can be loaded for the specified product. This is useful
when you want to automatically detect the newest available ACS release
instead of hard-coding a year.
}
\details{
The function checks up to 15 years in the past and returns the first year
for which \code{load_variables()} succeeds. For decennial frequency, only years
divisible by 10 are tested.

The function attempts to call \code{load_variables(year, product, cache = TRUE)}
for each candidate year, starting from the current year and moving backward.
The first year that loads successfully is returned.
}
\examples{
\dontrun{
latest_acs_year()
latest_acs_year(product = "acs5")
latest_acs_year(product = "acs5", frequency = "decennial")
}

}
